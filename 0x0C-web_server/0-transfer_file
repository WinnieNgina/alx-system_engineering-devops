!/usr/bin/env bash
#  transfers a file from our client to a server
len=$#

#check the total number of arguments

if [ $len -lt 3 ];
then
	echo "Usage: 0-transfer_file PATH_TO_FILE IP USERNAME PATH_TO_SSH_KEY"
else
	scp -o StrictHostKeyChecking=no $1 $2 -i $3
fi
